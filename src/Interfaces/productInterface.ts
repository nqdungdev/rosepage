export interface ICategory {
  category_id: string;
  category_name: string;
}

export interface IProduct {
  affiliate_link: string;
  category_id: string;
  image_link: string;
  product_id: string;
  product_link: string;
  product_name: string;
}
